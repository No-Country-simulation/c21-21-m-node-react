const LoaderAuth = () => {
    return (
       <button 
            className="bg-gray-200 flex animate-pulse text-transparent py-1 px-4 rounded-full">
            Cargando......
            </button>

   
    );
};

export default LoaderAuth;
